import ProductList from '../components/ProductList';

const Products = () => {
    return (
        <section className='pagesection'>
            <h1 className='pagetitle_without_bc'>Products to Sell</h1>
            <ProductList />
        </section>
    );
};

export default Products;